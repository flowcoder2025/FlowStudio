# 핸드오프 - 2026-01-27 (i18n Phase 1)

## 빌드 상태
- 타입 체크: ✅
- 빌드: ✅
- 린트: ✅ (warning만 - 기존 `<img>` 태그 관련, i18n 무관)

## 완료된 작업: Phase 1 (인프라 + 언어 토글 UI)

### 새로 생성된 파일
| 파일 | 설명 |
|------|------|
| `i18n/routing.ts` | next-intl 라우팅 설정 (ko 기본, en 추가) |
| `i18n/request.ts` | 서버 사이드 locale 감지 및 메시지 로딩 |
| `middleware.ts` | 언어 감지 미들웨어 (`localePrefix: 'as-needed'`) |
| `messages/ko/common.json` | 한국어 공통 번역 |
| `messages/en/common.json` | 영어 공통 번역 |
| `components/theme/language-toggle.tsx` | 언어 전환 드롭다운 (ThemeToggle 스타일) |
| `app/[locale]/layout.tsx` | 새로운 locale 기반 루트 레이아웃 |

### 수정된 파일
| 파일 | 변경 내용 |
|------|----------|
| `next.config.ts` | `createNextIntlPlugin` 추가 |
| `components/layout/Header.tsx` | `LanguageToggle` 컴포넌트 추가 |
| `app/[locale]/(auth)/login/page.tsx` | `<a>` → `<Link>` 태그 변경 (린트 수정) |

### 디렉토리 구조 변경
```
app/
├── [locale]/           ← 새 구조
│   ├── layout.tsx      ← 새 파일
│   ├── (main)/         ← 기존 app/(main)에서 이동
│   └── (auth)/         ← 기존 app/(auth)에서 이동
├── api/                ← 그대로 유지
├── globals.css         ← 그대로 유지
└── favicon.ico         ← 그대로 유지
```

### 설치된 패키지
```bash
npm install next-intl
```

## 현재 번역 키 구조
```json
{
  "nav": { "home", "gallery", "colorCorrection", "login", "logout", "settings", "myGallery", "pricing" },
  "theme": { "light", "dark", "system", "changeTheme" },
  "language": { "ko", "en", "changeLanguage" },
  "auth": { "profile" },
  "common": { "loading" },
  "metadata": { "title", "description" }
}
```

## URL 구조
- 한국어 (기본): `/`, `/gallery`, `/pricing` 등
- 영어: `/en`, `/en/gallery`, `/en/pricing` 등

## 다음 작업 (Phase 2: 핵심 페이지 번역)

### 수정 필요 파일
1. `components/layout/Header.tsx` - 네비게이션 텍스트 → `t()` 함수
2. `components/layout/Footer.tsx` - 회사 정보, 법적 링크 번역
3. `components/theme/theme-toggle.tsx` - 테마 옵션 라벨 번역
4. `app/[locale]/(main)/page.tsx` - 홈페이지 번역
5. `app/[locale]/(main)/pricing/page.tsx` - 가격 페이지 번역
6. `app/[locale]/(main)/gallery/page.tsx` - 갤러리 페이지 번역
7. `app/[locale]/(auth)/login/page.tsx` - 로그인 페이지 번역

### 번역 패턴 예시
```typescript
// 클라이언트 컴포넌트
'use client';
import { useTranslations } from 'next-intl';

export function Header() {
  const t = useTranslations('nav');
  return <Link href="/">{t('home')}</Link>;  // "홈" 또는 "Home"
}
```

## 미해결 이슈
- 없음

## 테스트 방법
1. `npm run dev` 실행
2. `/` 접속 → 한국어 UI
3. 헤더의 언어 토글 (🇰🇷) 클릭 → English 선택
4. `/en` 으로 리다이렉트 확인
5. 언어 토글 아이콘이 🇺🇸로 변경 확인

## 검증 완료
```bash
npm run build    # ✅ 성공
npx tsc --noEmit # ✅ 성공
npm run lint     # ✅ 에러 없음
```
