# 핸드오프 - 2026-01-28 (i18n Phase 4)

## 빌드 상태
- 타입 체크: ✅
- 빌드: ✅
- 린트: ✅ (경고 12개 - 기존 `<img>` 태그 관련, i18n 무관)

## 완료된 작업: Phase 4 (워크플로우 컴포넌트 번역)

### 수정된 파일
| 파일 | 변경 내용 |
|------|----------|
| `messages/ko/common.json` | actionCard, result, recommend, guide 섹션 추가 |
| `messages/en/common.json` | actionCard, result, recommend, guide 섹션 영어 번역 추가 |
| `components/workflow/ImmersiveActionSelect.tsx` | useTranslations 적용, 모든 UI 텍스트 번역 |
| `components/workflow/ImmersiveResult.tsx` | useTranslations 적용, 모든 UI 텍스트 번역 |
| `components/workflow/RecommendCard.tsx` | useTranslations 적용, 모든 UI 텍스트 번역 |
| `components/workflow/GuideChat.tsx` | useTranslations 적용, 모든 UI 텍스트 번역 |

### 추가된 번역 키 구조

#### workflow.ui 섹션 (확장)
```json
{
  "credits": "크레딧",
  "inputItems": "{count}개 입력 항목",
  "startWithThisStyle": "이 스타일로 시작하기",
  "viewOtherStyles": "다른 스타일 보기",
  "viewAsList": "리스트로 보기",
  "selectActionAria": "액션 선택",
  "swipeForOtherStyles": "스와이프해서 다른 스타일 보기",
  "arrowKeysForStyles": "← → 화살표 키로 스타일 탐색",
  "keyboardHintMove": "← → 이동",
  "keyboardHintSelect": "Enter 선택",
  "keyboardHintClose": "ESC 닫기"
}
```

#### workflow.actionCard 섹션 (신규)
```json
{
  "imageUpload": "이미지 업로드",
  "colorSelect": "색상 선택",
  "styleSelect": "스타일 선택",
  "aiGeneration": "AI 생성",
  "affordable": "저렴한 가격",
  "highQuality": "고품질"
}
```

#### workflow.result 섹션 (신규)
```json
{
  "generatedImages": "{count}장 생성됨",
  "creditsUsed": "{credits} 크레딧 사용",
  "createNew": "새로 만들기",
  "regenerate": "다시 생성",
  "download": "다운로드",
  "save": "저장",
  "saved": "저장됨",
  "share": "공유",
  "shareTitle": "FlowStudio 이미지",
  "urlCopied": "URL이 클립보드에 복사되었습니다",
  "resultAria": "생성 결과",
  "prevImageAria": "이전 이미지",
  "nextImageAria": "다음 이미지",
  "keyboardSwipeHint": "← → 키보드 또는 스와이프로 이동 • ESC로 닫기"
}
```

#### workflow.recommend 섹션 (신규)
```json
{
  "matching": "매칭",
  "recommended": "추천",
  "startNow": "시작하기",
  "select": "선택",
  "emptyMessage": "추천 워크플로우가 없습니다"
}
```

#### workflow.guide 섹션 (신규)
```json
{
  "title": "촬영 가이드",
  "welcomeMessage": "안녕하세요! 원하시는 이미지를 만들기 위해 몇 가지 질문을 드릴게요.",
  "completedMessage": "모든 정보 입력이 완료되었습니다! 이제 이미지를 생성할 준비가 되었어요. 🎉",
  "minLengthError": "조금 더 자세히 설명해주세요. (최소 {minLength}자 이상)",
  "maxLengthError": "텍스트가 너무 길어요. (최대 {maxLength}자)",
  "progress": "{current} / {total}",
  "inputPlaceholder": "입력해주세요...",
  "guideCompleted": "✅ 가이드가 완료되었습니다"
}
```

### 번역 적용 컴포넌트 상세

#### ImmersiveActionSelect.tsx
- 액션 카드 기능 태그 (이미지 업로드, 색상 선택 등)
- 크레딧/입력 항목 표시
- 버튼: 이 스타일로 시작하기, 다른 스타일 보기
- 네비게이션 힌트 (스와이프, 키보드)
- Aria 라벨

#### ImmersiveResult.tsx
- 헤더: 생성됨 개수, 크레딧 사용량
- 버튼: 새로 만들기, 다시 생성
- 이미지 액션: 다운로드, 저장, 공유
- 토스트: URL 복사 알림
- Aria 라벨 및 키보드 힌트

#### RecommendCard.tsx
- 추천 배지
- 매칭률 표시
- 버튼: 시작하기, 선택
- 빈 목록 메시지

#### GuideChat.tsx
- 타이틀: 촬영 가이드
- 환영 메시지
- 완료 메시지
- 유효성 검사 에러 메시지
- 진행률 표시
- 입력 placeholder
- 완료 상태 메시지

## 미완료 작업 (Phase 5에서 진행)

### 페이지 번역
- `app/[locale]/(main)/page.tsx` - 홈페이지
- `app/[locale]/(main)/pricing/page.tsx` - 가격 페이지
- `app/[locale]/(main)/gallery/page.tsx` - 갤러리 페이지
- `app/[locale]/(main)/result/page.tsx` - 결과 페이지

### 데이터 파일 번역 연동
- `lib/workflow/industries.ts` - 업종 데이터에서 번역 키 사용
- `lib/workflow/actions/*.ts` - 액션 데이터에서 번역 키 사용
- `lib/payment/config.ts` - 결제 설정에서 번역 키 사용

## 테스트 방법
1. `npm run dev` 실행
2. `/` 접속 → 업종 선택 → ImmersiveActionSelect 확인
3. 이미지 생성 → ImmersiveResult 확인
4. RecommendCard, GuideChat UI 텍스트 확인
5. 언어 토글 → `/en` 전환 후 영어 텍스트 확인

## 검증 완료
```bash
npm run build    # ✅ 성공
npx tsc --noEmit # ✅ 에러 0개
npm run lint     # ⚠️ 경고 12개 (기존 - Phase 범위 외)
```

## 기술 참고

### 번역 사용 패턴
```typescript
// 단일 namespace
const t = useTranslations("workflow.result");
return <span>{t("download")}</span>;

// 다중 namespace
const t = useTranslations("workflow.result");
const tUI = useTranslations("workflow.ui");
return <span>{tUI("close")}</span>;

// 파라미터 사용
t("generatedImages", { count: 4 })  // "4장 생성됨"
t("progress", { current: 2, total: 5 })  // "2 / 5"

// props로 번역 함수 전달 (내부 컴포넌트)
<ActionCard t={t} tCard={tCard} />
```

### 번역 함수 타입
```typescript
// 타입 정의
interface ActionCardProps {
  t: ReturnType<typeof useTranslations>;
  tCard: ReturnType<typeof useTranslations>;
}
```
