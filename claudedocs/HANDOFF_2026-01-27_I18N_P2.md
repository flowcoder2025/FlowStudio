# 핸드오프 - 2026-01-27 (i18n Phase 2)

## 빌드 상태
- 타입 체크: ✅
- 빌드: ✅
- 린트: ✅ (warning만 - 기존 `<img>` 태그 관련, i18n 무관)

## 완료된 작업: Phase 2 (핵심 컴포넌트 번역)

### 수정된 파일
| 파일 | 변경 내용 |
|------|----------|
| `components/layout/Header.tsx` | 모든 네비게이션 텍스트 → `t()` 함수 |
| `components/layout/Footer.tsx` | async 컴포넌트로 변환, `getTranslations` 사용 |
| `components/theme/theme-toggle.tsx` | 테마 옵션 라벨 번역 |
| `app/[locale]/(auth)/login/page.tsx` | 로그인 페이지 전체 번역 |
| `messages/ko/common.json` | 번역 키 확장 (login, footer, auth 섹션) |
| `messages/en/common.json` | 영어 번역 추가 |

### 번역 적용 컴포넌트 목록

#### Header.tsx
- 네비게이션: 홈, 갤러리, 색상 보정
- 프로필 메뉴: 내 갤러리, 설정, 로그아웃
- 로그인 버튼
- 모바일 메뉴 전체

#### Footer.tsx
- 법적 링크: 개인정보처리방침, 이용약관, 환불약관
- 회사 정보 라벨: 사업자등록번호, 대표

#### ThemeToggle
- 테마 옵션: 라이트, 다크, 시스템
- aria-label: 테마 변경

#### Login Page
- 제목, 부제목
- 에러 메시지
- 로그인 버튼: Google, Kakao
- 약관 동의 문구 (t.rich 사용)

### 현재 번역 키 구조
```json
{
  "nav": { "home", "gallery", "colorCorrection", "login", "logout", "settings", "myGallery", "pricing" },
  "theme": { "light", "dark", "system", "changeTheme" },
  "language": { "ko", "en", "changeLanguage" },
  "auth": { "profile", "termsAgreement", "termsOfService", "privacyPolicy" },
  "common": { "loading" },
  "footer": { "companyName", "businessNumber", "representatives", "privacyPolicy", "termsOfService", "refundPolicy", "allRightsReserved" },
  "login": { "title", "subtitle", "description", "continueWithGoogle", "continueWithKakao", "error" },
  "metadata": { "title", "description" }
}
```

## 사용된 번역 패턴

### 클라이언트 컴포넌트 (useTranslations)
```typescript
"use client";
import { useTranslations } from "next-intl";

export function Component() {
  const t = useTranslations();
  return <span>{t("nav.home")}</span>;
}
```

### 서버 컴포넌트 (getTranslations)
```typescript
import { getTranslations } from "next-intl/server";

export async function Component() {
  const t = await getTranslations("footer");
  return <span>{t("companyName")}</span>;
}
```

### Rich Text 번역 (t.rich)
```typescript
{t.rich("auth.termsAgreement", {
  terms: () => <Link href="/terms">{t("auth.termsOfService")}</Link>,
  privacy: () => <Link href="/privacy">{t("auth.privacyPolicy")}</Link>,
})}
```

## 다음 작업 (Phase 3 또는 추가 페이지 번역)

### 추가 페이지 번역 (선택적)
1. `app/[locale]/(main)/page.tsx` - 홈페이지
2. `app/[locale]/(main)/pricing/page.tsx` - 가격 페이지
3. `app/[locale]/(main)/gallery/page.tsx` - 갤러리 페이지

### Phase 3: 워크플로우 & 결제 시스템
1. `lib/workflow/industries.ts` - 업종명
2. `lib/workflow/actions/*.ts` - 액션명
3. `lib/payment/config.ts` - 결제 플랜명
4. `components/workflow/*.tsx` - 워크플로우 UI
5. `components/payment/*.tsx` - 결제 모달

## 미해결 이슈
- 없음

## 테스트 방법
1. `npm run dev` 실행
2. `/` 접속 → 한국어 헤더/푸터 확인
3. 언어 토글 클릭 → `/en` 전환
4. 영어 헤더/푸터/테마 옵션 확인
5. `/en/login` 접속 → 영어 로그인 페이지 확인

## 검증 완료
```bash
npm run build    # ✅ 성공
npx tsc --noEmit # ✅ 성공
npm run lint     # ✅ 에러 없음
```
